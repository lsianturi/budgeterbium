import * as actions from './actions'
import getters from './getter'
import mutations from './mutations'

const state = {
  accounts: {}
}

export default {
  state,
  actions,
  mutations,
  getters
}
